extends layout 

block content 
    h1 New Message
    form(method="POST", action="/new") 
        input(name="author" ,type="text", placeholder="Name")
        br
        br
        textarea(name="message", cols="30", rows="10", placeholder="Message")
        br
        br
        input(type="submit", value="Submit")
    br
    a(href='/')
        button Go to Message Board
